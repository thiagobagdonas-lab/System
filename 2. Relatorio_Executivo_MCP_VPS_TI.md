# Relatório Executivo — Infraestrutura MCP Server
## Reunião Técnica com Equipe de TI

**Data:** 26/06/2026 (alinhado ao Briefing TI v2 — revisão da versão de 24/06/2026)
**Responsável:** Thiago Bagdonas — IA & Dados, Setter
**Classificação:** Uso interno

---

## 1. Contexto e Objetivo

A Setter está implementando um servidor MCP (Model Context Protocol) próprio como parte da adoção estratégica da plataforma Claude (Anthropic) para operações de M&A. O MCP é o protocolo padrão que permite ao Claude acessar dados internos, executar automações e consultar bancos de dados em tempo real durante as análises dos analistas.

**Objetivo desta reunião:** Alinhar a divisão de responsabilidades entre TI e a área de IA/Dados para o provisionamento, segurança e manutenção contínua da infraestrutura de VPS que hospedará o servidor.

**Resultado esperado:** TI entrega uma VPS hardened e assume a manutenção de segurança da infraestrutura. A área de IA/Dados assume o deploy e manutenção da aplicação, com posse exclusiva das chaves de API.

---

## 2. O Que é o MCP Server (contexto técnico)

O MCP Server é um processo Python (FastMCP + Uvicorn) que roda na VPS e expõe endpoints HTTP autenticados. Os clientes são os aplicativos Claude (Claude Desktop, Claude Code, Claude Cowork) instalados nas máquinas dos usuários da Setter.

```
[Claude — máquinas dos analistas]
        │
        │  HTTPS + Bearer Token
        ▼
[nginx — reverse proxy + TLS termination]
        │
        │  HTTP interno (127.0.0.1:8000)
        ▼
[Uvicorn → FastMCP Application]
        │
        ├── SQLite (banco interno de deals)
        ├── Scripts Python/Shell (automações ETL)
        └── Audit log (append-only, imutável)
```

**O que o servidor expõe:**
- **Tools:** funções que o Claude pode invocar — inserir deals, disparar ETL, gerar relatórios
- **Resources:** consultas read-only ao banco — pipeline de deals, valuations, research setorial
- **Health endpoint:** `GET /health` — sem autenticação, para monitoramento

**O que o servidor NÃO faz:**
- Não acessa a internet (apenas recebe conexões de entrada)
- Não armazena dados em memória entre sessões (stateless por request)
- Não executa código arbitrário (allowlist explícita de scripts autorizados)

---

## 3. Divisão de Responsabilidades

Esta é a separação proposta entre TI e IA/Dados:

### 3.1 Responsabilidade do TI — Infraestrutura e Segurança

| Domínio | Tarefa | Periodicidade |
|---|---|---|
| **Provisionamento** | Criação da VPS com Ubuntu 24.04 LTS | Uma vez |
| **Acesso inicial** | Conta de serviço `setter-app` (sem sudo) + admins `setter-admin`/`ti-admin` (com sudo), chaves SSH | Uma vez |
| **Docker engine** | Instalar e habilitar Docker (a aplicação roda em container — TI não toca no conteúdo) | Uma vez |
| **SSH hardening** | Desabilitar root login, desabilitar password auth | Uma vez |
| **Firewall (UFW)** | Liberar 22 e 51820/UDP (WireGuard); 80/443 não públicas | Uma vez |
| **WireGuard (VPN)** | Configurar túnel — único ponto de entrada público (caminho único v2) | Uma vez |
| **Fail2ban** | Configurar banimento por tentativas SSH | Uma vez |
| **Certbot / TLS** | Emitir/renovar certificado Let's Encrypt para `mcp.setter.net.br` via **DNS-01** (80/443 não públicas) | Automático (renovação 90 dias) |
| **OS updates** | `unattended-upgrades` para patches de segurança | Automático (diário) |
| **logrotate** | Rotação dos logs da aplicação (caminho fornecido pela IA/Dados) | Uma vez |
| **Snapshots** | Snapshot automático semanal via painel do provedor | Automático (semanal) |
| **Agendamento de backup** | Agendar (cron) o script de backup da IA/Dados e enviar a saída para fora da VPS | Automático (diário) |
| **Monitoramento** | Alertas de CPU/memória/disco + health check do `/health` da aplicação | Contínuo |
| **Acesso de emergência** | Console de recuperação via provedor (acesso out-of-band, com MFA) | Sob demanda |
| **Auditoria de acesso** | Revisão mensal de `/var/log/auth.log` e eventos fail2ban | Mensal |

### 3.2 Responsabilidade da Área de IA/Dados — Aplicação

| Domínio | Tarefa |
|---|---|
| **Deploy** | Imagem e containers Docker, dependências Python (dentro da imagem) |
| **Configuração da aplicação** | Arquivo `.env` com as API keys (posse exclusiva) |
| **Banco de dados** | Schema SQLite, rotação de dados e **lógica do script de backup** (`.backup`/`.dump` — agendamento e envio externo são do TI) |
| **systemd / container** | Unit do serviço com `Restart=always` + proteção de crash-loop; start/stop/restart da aplicação |
| **nginx** | Configuração do `server block` para o MCP |
| **Manutenção do código** | Updates de versão do FastMCP, novas tools, novos scripts |
| **API keys** | Geração, rotação e distribuição das chaves de autenticação |
| **Audit log** | Monitoramento e análise de eventos de auditoria da aplicação |

### 3.3 Fronteira crítica — o que cada parte NUNCA toca

> **TI não acessa:**
> - O arquivo `config/.env` (contém as API keys)
> - O conteúdo do banco SQLite (`data/setter.db`)
> - Os audit logs da aplicação (`logs/audit.log`)
>
> **IA/Dados não assume:**
> - Manutenção do kernel e pacotes do OS
> - Regras de firewall, fail2ban e configuração do WireGuard
> - Renovação de certificados TLS
> - Acesso de emergência via console do provedor

Esta separação respeita o princípio de menor privilégio e garante que nenhuma das partes precisa da outra para executar suas funções rotineiras.

> **Ressalva de segredos (v2):** a regra "TI não acessa o `.env`" vale no sistema em execução, mas **quem controla snapshots ou o console do provedor consegue ler o disco inteiro** — inclusive o `.env`. Por isso a conta do provedor é tratada como superfície que carrega segredos: **MFA obrigatório** e credenciais em cofre. Ver Briefing TI v2, seção 7.2.

---

## 4. Especificações Técnicas da VPS

### 4.1 Requisitos mínimos

| Recurso | Mínimo | Recomendado |
|---|---|---|
| vCPU | 1 | 2 |
| RAM | 2 GB | 4 GB |
| Disco SSD | 20 GB | 40 GB |
| Rede | 100 Mbps | 200 Mbps |
| OS | Ubuntu 24.04 LTS | Ubuntu 24.04 LTS |
| IP | 1 IPv4 estático | 1 IPv4 estático |

**Estimativa de custo:** R$ 80–150/mês (Hetzner CX22 ou DigitalOcean equivalente). Custo aumenta apenas se SQLite ultrapassar ~50 GB — improvável no horizonte de 12 meses.

### 4.2 Provedor recomendado

- **Hetzner Cloud** (Helsinki ou Ashburn): melhor custo-benefício, ~€4/mês para CX22
- **DigitalOcean** (São Paulo): latência menor para o Brasil, ~$24/mês para Droplet equivalente

Ambos fornecem console out-of-band, snapshots automatizados e IPv4 estático incluso. **MFA na conta do provedor é obrigatório** (a conta controla snapshots e console).

### 4.3 Configuração de rede necessária

- **DNS:** o TI confirma e entrega o IP estático; o time interno da Setter cria o registro A `mcp.setter.net.br` → IP da VPS (o TI não acessa o painel DNS da Setter)
- **Portas abertas no firewall externo:** 22 (SSH) e 51820/UDP (WireGuard) — **80/443 não são públicas** (acesso via túnel)
- **Nenhuma outra porta deve ser exposta** — o processo da aplicação escuta exclusivamente em `127.0.0.1:8000` e só é alcançável via VPN

---

## 5. Modelo de Segurança

### 5.1 Camadas de defesa

```
Internet
   │
   ├─ [UFW] Bloqueia tudo exceto 22 (SSH) e 51820/UDP (WireGuard)
   │
   ├─ [WireGuard] Único ponto de entrada público; app só alcançável pelo túnel
   │
   ├─ [Fail2ban] Bane IPs após 3 falhas SSH em 10 min
   │
   ├─ [nginx] TLS 1.2+, HSTS, rate limiting no endpoint do MCP (10 req/s)
   │
   ├─ [FastMCP + StaticTokenVerifier] Bearer token obrigatório por tier
   │       ├─ Tier read  → analistas (consultas)
   │       ├─ Tier write → automações internas
   │       └─ Tier admin → responsável IA/Dados
   │
   ├─ [SQLite WAL + queries parametrizadas] Zero SQL injection possível
   │
   └─ [Audit log append-only] chattr +a — nem root deleta
```

### 5.2 Autenticação — o que TI precisa saber

O servidor usa Bearer tokens (API keys) gerados e gerenciados exclusivamente pela área de IA/Dados. O TI **não precisa e não deve** conhecer os valores dessas chaves. A autenticação é tratada na camada de aplicação (FastMCP), não no nginx.

O nginx valida apenas TLS e aplica rate limiting. Não faz autenticação de conteúdo.

### 5.3 Vetor de ataque mapeado — prompt injection via MCP

O principal risco específico desta aplicação é **prompt injection**: um documento malicioso aberto por um analista poderia, em tese, tentar instruir o Claude a executar queries não autorizadas via MCP. As defesas implementadas:

- Queries SQL sempre parametrizadas (sem interpolação de string)
- Allowlist explícita de scripts que podem ser invocados
- Parâmetros passados via stdin JSON, nunca como args de shell
- Audit log de toda operação, incluindo parâmetros recebidos
- Tools de escrita requerem campo `justificativa` obrigatório

---

## 6. Checklist de Entrega — TI para IA/Dados

Ao final do provisionamento, o TI deve entregar à área de IA/Dados. Esta é a versão resumida — o checklist completo e canônico está no **Briefing TI v2, seção 8**:

- [ ] VPS com **Ubuntu 24.04 LTS**, IP estático e **MFA ativo na conta do provedor**
- [ ] Conta de serviço `setter-app` (sem sudo, fora do grupo docker) + admins `setter-admin`/`ti-admin`
- [ ] Confirmação de que `root` login e autenticação por senha estão desabilitados
- [ ] UFW ativo — somente 22 e 51820/UDP; **80/443 não públicas** (`sudo ufw status verbose`)
- [ ] Fail2ban ativo — status via `sudo fail2ban-client status`
- [ ] `unattended-upgrades` configurado e ativo
- [ ] **Docker** instalado e ativo; **WireGuard** ativo com peer de teste validado
- [ ] Registro DNS `mcp.setter.net.br → IP` propagado (criado pela Setter)
- [ ] Certbot instalado e renovação por **DNS-01** validada
- [ ] nginx instalado e rodando (sem `server block` — isso é responsabilidade da IA/Dados)
- [ ] `logrotate`, **monitor de saúde do `/health`**, **backup do banco agendado + envio externo + teste de restauração** e **snapshot semanal** configurados
- [ ] Monitoramento de CPU/RAM/disco com alerta para **canal/lista compartilhada** da IA/Dados (não e-mail pessoal único)
- [ ] Documento **as-built** e acesso out-of-band documentados e compartilhados em cofre

---

## 7. Manutenção Contínua — O que o TI Mantém

### Rotina automática (sem intervenção manual esperada)

- **Patches de segurança:** `unattended-upgrades` aplica automaticamente patches do kernel e pacotes críticos
- **Renovação TLS:** Certbot renova o certificado a cada 60–90 dias via `cron`, usando **DNS-01** (80/443 não públicas) — falha gera alerta
- **Backup do banco:** script da IA/Dados executado por `cron` e enviado para fora da VPS — falha gera alerta
- **Snapshot:** imagem semanal do host via painel do provedor (complementa, não substitui o backup do banco)
- **logrotate + health check:** logs rotacionados; `/health` monitorado, com alerta à IA/Dados se a aplicação não responder
- **Fail2ban:** opera de forma autônoma, bane e desbane IPs conforme regras configuradas

### Rotina mensal (intervenção ativa do TI)

- Revisão de `/var/log/auth.log` — identificar tentativas de acesso suspeitas
- `sudo fail2ban-client status` — revisar IPs banidos recorrentes
- `df -h` — verificar crescimento de disco (alertar IA/Dados se > 70%)
- `sudo apt list --upgradable` — verificar se há pacotes pendentes fora do escopo do `unattended-upgrades`

### Atualizações de kernel com reboot

Quando o kernel for atualizado (raro, mas necessário), o processo é:

1. TI notifica IA/Dados com 24h de antecedência
2. IA/Dados confirma janela de manutenção (evitar horário comercial)
3. TI executa `sudo reboot`
4. systemd reinicia automaticamente o serviço MCP após o boot — **desde que a unit tenha `Restart=always`** + proteção de crash-loop (validado no checklist)
5. Validação de saúde: no host via `curl http://127.0.0.1:8000/health` e, de fora, conectado ao túnel (`GET` pelo IP do WireGuard) — a URL pública não responde fora da VPN

---

## 8. Modelo de Comunicação e Escalação

| Situação | Responsável primário | Ação |
|---|---|---|
| VPS inacessível via SSH | TI | Acesso via console out-of-band, investigar SO |
| Serviço MCP fora do ar (nginx OK, SSH OK) | IA/Dados | `sudo systemctl restart setter-mcp` |
| Certificado TLS expirado | TI | `sudo certbot renew` |
| Tentativas de acesso suspeitas (auth log) | TI | Revisar fail2ban, reforçar regras UFW se necessário |
| Disco acima de 80% | TI alerta → IA/Dados resolve | IA/Dados limpa backups antigos ou expande banco |
| Rotação de API keys | IA/Dados | Atualiza `.env`, reinicia serviço — TI não é envolvido |
| Vulnerabilidade crítica (CVE) no OS | TI | Patch e reboot em janela de manutenção acordada |
| Nova automação ou tool no servidor | IA/Dados | Deploy via git pull, restart do serviço — TI não é envolvido |

---

## 9. Perguntas para a Reunião

> **Já decidido na v2 do Briefing TI:** base **Ubuntu 24.04**, arquitetura em **Docker**, VPN via **WireGuard** (caminho único), DNS criado pela Setter, e o **backup do banco** (TI agenda o script da IA/Dados + envio externo; snapshot semanal obrigatório do TI). O item de backup abaixo deixa de ser pergunta.

Itens a definir durante a conversa com o TI:

1. **Provedor:** Hetzner ou DigitalOcean? Há preferência ou contrato existente?
2. **DNS:** Quem administra o domínio `setter.net.br`? TI ou terceiro? (o registro A é criado pela Setter)
3. **Monitoramento:** O TI já tem ferramenta de monitoramento (Zabbix, Grafana, Datadog)? Integrar ou usar alerta básico por email?
4. **Janela de manutenção padrão:** Qual horário é aceitável para reboots de kernel?
5. **Acesso de emergência:** Como documentar e compartilhar credenciais do console do provedor de forma segura (cofre + MFA)?
6. ~~**Backup offsite:** O TI assume backup dos snapshots da VPS, ou IA/Dados cuida disso separadamente do backup do banco?~~ **— Resolvido na v2:** snapshot semanal é do TI; o backup do banco usa script da IA/Dados agendado pelo TI via cron e enviado para fora da VPS, com teste de restauração trimestral (RPO ≤ 24h / RTO ≤ 4h). Resta confirmar o destino externo.
7. **Cobertura/SLA fora do horário:** Qual o tempo de resposta por severidade e a cobertura fora do horário comercial? (ver SLA no Briefing TI v2, seção 9)
8. **Prazo:** Quando o TI consegue entregar a VPS hardened e pronta para deploy?

---

## 10. Cronograma Proposto

```
Semana 1
  TI:       Provisionar VPS (24.04), MFA no provedor, contas (setter-app/admins)
  TI:       Hardening SSH, UFW (22 + 51820/UDP), Fail2ban, unattended-upgrades
  TI:       Instalar Docker, nginx, Certbot (DNS-01) e configurar WireGuard
  Setter:   Criar registro A mcp.setter.net.br (TI entrega o IP)
  TI:       logrotate, snapshot semanal, agendamento de backup + envio externo
  TI:       Entrega do checklist (seção 6) + documento as-built para IA/Dados

Semana 2
  IA/Dados: Deploy da aplicação (container Docker), nginx server block, TLS
  IA/Dados: Unit com Restart=always; script de backup (.backup/.dump)
  IA/Dados: Testes com MCP Inspector; integração com Claude Code (admin)

Semana 3
  IA/Dados: Integração com Claude Desktop dos analistas (peers WireGuard)
  Ambos:    Validação conjunta: health check, teste de restauração, kill do processo
  Ambos:    Confirmar SLA de manutenção (seção 9 do Briefing TI v2)
```

---

*Relatório preparado por Thiago Bagdonas — IA & Dados, Setter*
*Referência técnica: OPORD SETTER-MCP-001 v2.0 (documento interno)*
